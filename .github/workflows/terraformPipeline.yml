name: 'Terraform-workflow'

on: [push, pull_request]
    
jobs:

  build-and-test:
    uses: LuckyDucky583/terraform-github-actions/.github/workflows/.terraform-aws.yml@feat/change-folder-structure
    with:
      working-directory: ./src/infrastructure/envs/demo
    secrets: inherit